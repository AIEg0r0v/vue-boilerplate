.tag {
  white-space: nowrap;
  padding: 6px 20px;
  border-radius: 50px;
  color: rgba(51, 60, 73, 0.8);

  font-size: 13px;
  background: #F3F6F9;
  display:flex;
  align-content: center;
  justify-content: space-between;
}

.tag-icon {
  margin-left: 0.625rem;
}