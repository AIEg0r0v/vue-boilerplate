.user-component {
  display: flex;
  &:hover {
    .name{
      color: rgba(255, 255, 255, 0.5);
    }
  }
}

.name {
  font-weight: 600;
  font-size: 18px;
  line-height: 25px;
  color: #fff;
  margin-left: 1rem;
  display: flex;
  align-items: center;
}